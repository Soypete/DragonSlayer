{
  "name": "practice-dungeon",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A training ground for fledgling dragonslayers. Some halls are warded by tests; others crawl with dragons.",
  "scripts": {
    "test": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.1.9",
    "vitest": "^4.1.9"
  }
}
